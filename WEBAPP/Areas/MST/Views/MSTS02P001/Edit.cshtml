@using FluentBootstrap
@using WEBAPP.Helper
@using DataAccess.MST
@using UtilityLib

@model MSTS02P001Model
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutChildPage.cshtml";
}


@using (var form = Html.Bootstrap().Form().SetId("form1").SetHorizontal().Begin())
{
    @Html.AntiForgeryToken()
    using (var row1 = Html.Bootstrap().GridRow().Begin())
    {
        using (row1.GridColumn(6).Begin())
        {
            @form.InputFor(m => m.YEAR, isRequired: true, addValidateMessage: true).SetHelp("Format : YYYY Example: (2014)").SetMaxLength(4).SetReadonly()
        }
        using (row1.GridColumn(6).Begin())
        {
            @form.InputNumberFor(m => m.MANDAY_VAL, digits: 1, isRequired: true)
        }
    }
    using (var row1 = Html.Bootstrap().GridRow().Begin())
    {
        using (row1.GridColumn(6).Begin())
        {
            @*@form.InputFor(m => m.YEAR).SetHelp("Format : YYYY Example: (2014)").SetMaxLength(4)*@
            @form.SelectFor(t => t.APP_CODE, Model.APP_CODE_MODEL.ToSelectList("Value", "Text", Model.APP_CODE), IsSearch: true)
        }
    }
}

<script>
    var isUsed = '@Model.IS_USE';
    if (isUsed != 'F') {
        $("#MANDAY_VAL").attr("disabled", "disabled");
    }
</script>
